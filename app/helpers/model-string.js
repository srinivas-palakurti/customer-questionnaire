import { helper } from '@ember/component/helper';

export default helper(function modelString(params) {
  return params;
});



// export default helper(function ifCondition(params) {
//   return  equal(params[0], params[1]);
// });

